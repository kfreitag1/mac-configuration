---
name: pr
description: Read and understand the current graphite branch and its changes, then follow user instructions for working on the PR.
---

Read and understand the current graphite branch, its stack position, and all changes. Use the graphite skill and `gt`/`gh`/`git` CLI as needed to build full context. If you are on an empty branch (to enable parallel developmenmt with worktrees), review in depth the branch underneath instead. Review the outline for the entire stack and explore other branches in depth AS NEEDED if relevent.

Then follow the user's instructions:

$ARGUMENTS
